First step you need to install python packages placed in 'requirements.txt' =>>> to do it type in terminal pip install -r requirements.txt
Second, to run the server in terminal py manage.py runiserver. To stop server press ctrl C
To see the admin page type in browser's url /admin after localhost
